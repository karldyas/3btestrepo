Serves a single "Hello, world" page.

- Trigger: HTTP GET on `/hello-world` (space members only).
- Renders a React app defined in [App.tsx](App.tsx), styled with Tailwind.
- No external services, no inputs, no side effects.
