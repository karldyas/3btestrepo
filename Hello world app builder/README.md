A minimal hello-world web page.

Visiting `/hello-world` runs the single [Hello world](<Hello world/App.tsx>) step, which renders a React page and returns it as the HTTP response. The route is restricted to members of the space.

No connectors, inputs, or side effects.
