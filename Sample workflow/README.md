A minimal sample workflow with a single step, useful as a starting point or smoke test.

There is no trigger — run [Sample step](<Sample step/script.ts>) manually. It takes no input and prints a JSON object with a greeting and the current timestamp. No external services, no side effects.
